# - question: "What compute resources are available for Mila students?"
#   options:
#     - "A heterogeneous, Slurm-managed cluster featuring multi-core CPUs and various generations of GPUs (including H100, L40S, A100, and RTX8000)"
#     - "Only legacy CPU nodes featuring integrated graphics capabilities"
#     - "Distributed cloud computing credits restricted to AWS infrastructure exclusively"
#     - "A single homogeneous supercomputer entirely composed of V100 architectures"
#     - "Experimental quantum computing accelerators only"
#   answer: 0
#   skills:
#     - mila-run-jobs
#
# - question: "How many GPUs am I allowed to use at any given time for interactive development?"
#   options:
#     - "Mila attempts to honor a strict guarantee of giving at minimum one GPU per student, all the time, specifically for interactive mode usage"
#     - "You are restricted to exactly zero GPUs for interactive sessions to save resources"
#     - "Exactly 8 GPUs at all times, regardless of the task"
#     - "You are restricted to using GPUs during non-interactive batch jobs only"
#     - "As many as you want without any queuing limits"
#   answer: 0
#   skills:
#     - mila-run-jobs
#
- question: "Where should I store datasets on the Mila cluster to ensure maximum I/O performance during a training job?"
  options:
    - "$SLURM_TMPDIR"
    - "$HOME"
    - "/network/projects"
    - "$ARCHIVE"
    - "$SCRATCH"
  answer: 0
  skills:
    - mila-run-jobs

- question: "Where should I setup my code repositories on the Mila cluster?"
  options:
    - "$HOME"
    - "$SLURM_TMPDIR"
    - "/network/datasets"
    - "$SCRATCH"
    - "B and D"
  answer: 0
  skills:
    - mila-run-jobs

- question: "How can I use Visual Studio Code to develop code on the Mila cluster?"
  options:
    - "By installing it locally on your machine and utilizing the 'Remote-SSH' extension to connect seamlessly to `mila-cpu` or environments instantiated by `mila code`"
    - "By running the heavy VSCode graphical user interface directly on the login node"
    - "VSCode is strictly banned; researchers are forced to use terminal-based vim"
    - "By downloading a specific VSCode cluster software module and executing it locally"
    - "By using the dashboard.server.mila.quebec web-based IDE"
  answer: 0
  skills:
    - mila-run-jobs
    - mila-connect-cluster

- question: "Can I run claude on the Mila cluster login nodes?"
  options:
    - "No, running heavy, persistent, or agentic processes (like Claude Code) on shared login nodes is strictly prohibited as it heavily strains shared CPU and RAM resources"
    - "Yes, it is highly encouraged to run heavy agentic processes on the login node"
    - "Yes, as long as it is a short, isolated prompt"
    - "Yes, but only if you execute it using the `--dangerously-skip-permissions` flag"
    - "A and D"
  answer: 0
  skills:
    - mila-run-jobs
    - mila-connect-cluster

- question: "How do I execute my first job on the cluster?"
  options:
    - "By deeply reading the 'Run Your First Job' minimal example, preparing a correctly formatted `job.sh` shell script detailing resource requests, and submitting it to the active queue via `sbatch`"
    - "By running the python script directly on the login node and ignoring the warnings"
    - "By physically pressing the execute button located on the Grafana dashboard"
    - "By calling the IDT team and reading the code to them over the phone"
    - "By using the `srun` command without allocating any hardware resources first"
  answer: 0
  skills:
    - mila-run-jobs

- question: "How do I train my first model on the cluster?"
  options:
    - "By strictly following the 'Train Your First Model' guide, which comprehensively details allocating a GPU, optimally staging the shared CIFAR-10 dataset exclusively into `$SLURM_TMPDIR`, and seamlessly launching a batch job"
    - "By running the training code directly on your local laptop and connecting to the data via a slow VPN"
    - "By copying the massive network weights directly to your `$HOME` directory and executing it entirely on the login node"
    - "By utilizing the external TamIA cluster interactively"
    - "By commanding the Claude Code agent to execute it via the `--dangerously-skip-permissions` flag"
  answer: 0
  skills:
    - mila-run-jobs
#
# - question: "What is the difference between an interactive and batch job?"
#   options:
#     - "Interactive jobs provide a live, responsive terminal session for real-time debugging and command execution; batch jobs run autonomously in the background via submitted shell scripts (`sbatch`)"
#     - "Batch jobs strictly require GPUs; interactive jobs run exclusively on CPUs only"
#     - "Interactive jobs are entirely immune to preemption; batch jobs are not"
#     - "Interactive jobs can only execute on TamIA; batch jobs execute strictly on Mila"
#     - "There is absolutely no functional difference between the two paradigms"
#   answer: 0
#   skills:
#     - mila-run-jobs
#
# - question: "What is a Slurm cluster?"
#   options:
#     - "A massive system managed by a workload/batch scheduler where users must explicitly allocate precise resources (like CPUs, memory, and GPUs) before executing jobs, thereby preventing catastrophic hardware overallocation"
#     - "A highly specialized database structure utilized exclusively for storing large image datasets"
#     - "A makeshift cluster of purely consumer-grade laptops connected via local WiFi"
#     - "An interactive frontend website built strictly for creating AI-generated art"
#     - "A specific, deprecated type of Python virtual environment"
#   answer: 0
#   skills:
#     - mila-run-jobs

- question: "What is a Compute Node?"
  options:
    - "A dedicated, isolated server within the broader cluster where actual physical resources (CPUs, GPUs, RAM) are allocated exclusively to your specific job via the Slurm workload scheduler"
    - "A node explicitly reserved for handling web traffic and routing SSH tunneling"
    - "The exact node you first land on when typing `ssh mila` in your local terminal"
    - "A node strictly utilized for storing long-term, cold archive data"
    - "B and D"
  answer: 0
  skills:
    - mila-run-jobs
#
# - question: "Why do I need to allocate resources before I can run a job?"
#   options:
#     - "Because the Slurm workload manager relies on this to prevent users from overloading shared hardware, guaranteeing you receive dedicated, un-interfered access to the specific CPUs and GPUs requested"
#     - "To inform the centralized billing department exactly how much to charge your credit card"
#     - "Because the underlying C++ compiler will categorically refuse to execute unless you provide the exact execution time"
#     - "To strictly satisfy external DRAC governmental compliance laws"
#     - "It is an outdated, legacy rule that no one formally follows anymore"
#   answer: 0
#   skills:
#     - mila-run-jobs

- question: "My jobs are taking a long time to start running on the cluster. What can I do?"
  options:
    - "Ensure you are utilizing optimal resource requests, avoid relying solely on heavily constrained 'unkillable' partitions, and verify actual GPU availability via the Grafana dashboard"
    - "Submit the exact same job script 100 times to overwhelm the scheduler and force it to pick it up"
    - "Run the massive training script directly on the login node instead of waiting in the queue"
    - "Email the IT team demanding they manually override the queue to start your job"
    - "Use the `--dangerously-skip-permissions` flag to bypass the Slurm wait times"
  answer: 0
  skills:
    - mila-run-jobs
#
# - question: "Why are my jobs being interrupted on the Mila cluster before they have a chance to finish?"
#   options:
#     - "Because of strict cluster preemption policies (if operating on a preemptible partition), reaching the explicitly requested maximum time limit, or encountering a fatal CUDA OOM (Out of Memory) error"
#     - "Because you accidentally used the `short-unkillable` partition"
#     - "Because the cluster automatically resets and purges all jobs every hour on the hour"
#     - "Because an administrator physically pressed the power button on the server rack"
#     - "Because you optimally stored your training data in `$SLURM_TMPDIR`"
#   answer: 0
#   skills:
#     - mila-run-jobs

- question: "Which of the following is NOT a good practice for development workflow on the Mila cluster?"
  options:
    - "Executing a 12-hour PyTorch distributed training loop directly on the shared login node to entirely avoid waiting in the Slurm queue"
    - "Utilizing `mila code` to securely and optimally attach a VSCode instance directly to a GPU compute node"
    - "Preemptively copying massive datasets to the local NVMe `$SLURM_TMPDIR` before beginning a heavy training loop"
    - "Employing `uv` to rapidly resolve and cleanly install Python package dependencies"
    - "Systematically using proper checkpointing logic to continuously save model state dicts to `$SCRATCH` in case of aggressive preemption"
  answer: 0
  skills:
    - mila-run-jobs
    - mila-connect-cluster
#
# - question: "Who can help me with my technical questions, for example, to help me optimize my pytorch code?"
#   options:
#     - "The IDT team via their established office hours or specialized Slack channels"
#     - "The IT support desk"
#     - "The network infrastructure administrators"
#     - "The desk reservation policy committee"
#     - "None of the above"
#   answer: 0
#   skills:
#     - mila-run-jobs
