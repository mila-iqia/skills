- question: "How do I connect to the cluster?"
  options:
    - "By using SSH via a local terminal or by utilizing the `milatools` Python package (e.g., `mila init`, `mila-cpu`, or `mila code`)"
    - "Through a specialized web browser portal exclusively"
    - "By physically plugging a USB drive directly into the server rack"
    - "Using a standard FTP client only"
    - "By connecting directly through the dashboard.server.mila.quebec diagnostic URL"
  answer: 0
  skills:
    - mila-connect-cluster
    - mila-local-setup

- question: "How do I connect to the cluster?"
  options:
    - "By utilizing `ssh mila` from your local terminal (if manually configured) or by leveraging the automated tools provided by installing the `milatools` framework locally"
    - "By navigating a web browser to a hidden IP address"
    - "By calling the IT desk and asking them to open a remote desktop connection"
    - "By running `sbatch connect.sh`"
    - "A and B"
  answer: 0
  skills:
    - mila-connect-cluster
    - mila-local-setup

- question: "How do I install uv on the cluster?"
  options:
    - "By running the curl command from the uv docs on a login node"
    - "By submitting an IT ticket demanding they install it globally for all users"
    - "It is already installed globally by default on all nodes, meaning you do not need to install it"
    - "By using the `module load uv` command on the login node"
    - "The `uv` package manager is strictly banned on the cluster infrastructure"
  answer: 0
  skills:
    - mila-connect-cluster

- question: "If I have some SSH issues when connecting to compute nodes, which Slack channel should I ask for help in?"
  options:
    - "#mila-cluster"
    - "#tamia-cluster"
    - "#pytorch"
    - "#compute-canada"
    - "#random"
  answer: 0
  skills:
    - mila-connect-cluster

- question: "Is it okay to direct-message an IT or IDT team member on Slack if I have a problem?"
  options:
    - "No, you must utilize the public channels (like #mila-cluster), physically attend office hours, or officially open an IT support ticket"
    - "Yes, bypassing channels is the fastest and highly recommended way to get immediate help"
    - "Yes, but strictly only after midnight to avoid daytime congestion"
    - "Yes, but exclusively for advanced PyTorch bugs"
    - "No, the entire Slack platform is strictly forbidden for any technical communication"
  answer: 0
  skills:
    - mila-connect-cluster

- question: "Who should I reach out to if I have trouble connecting to the cluster with `mila code`?"
  options:
    - "IT first, since they manage SSH connection issues. IDT only as a last resort (or via Slack)."
    - "The IDT Team via Slack or office hours"
    - "The IT Team, because it involves SSH tunneling and jump host infrastructure"
    - "Compute Canada"
    - "No one, the specific command has been deprecated and is no longer supported"
  answer: 0
  skills:
    - mila-connect-cluster
    - mila-run-jobs

- question: "Can I run Visual Studio Code on the Mila cluster login nodes?"
  options:
    - "No, VSCode IDE backend servers are exceptionally heavy processes. You must use `mila code` or the specific `mila-cpu` SSH entry to run VSCode securely on an isolated compute node"
    - "Yes, it is perfectly safe and won't noticeably consume shared resources"
    - "Yes, but strictly only if you manually disable all installed extensions first"
    - "No, Visual Studio Code is strictly banned across the entire Mila organizational infrastructure"
    - "Yes, but only if you launch the GUI via the external DRAC portal"
  answer: 0
  skills:
    - mila-connect-cluster
    - mila-run-jobs

- question: "What is a Login node?"
  options:
    - "The primary entry point to the cluster infrastructure, used exclusively for lightweight administrative tasks like navigating directories, compiling basic code, and submitting batch jobs to the Slurm queue"
    - "A heavily provisioned node dedicated entirely to running 5-day ML training jobs"
    - "A specific compute node that natively has 8x H100 GPUs attached to it"
    - "A node that is permanently and physically disconnected from the internet"
    - "A massive storage array that exclusively holds the `$SCRATCH` partition"
  answer: 0
  skills:
    - mila-connect-cluster
    - mila-run-jobs
